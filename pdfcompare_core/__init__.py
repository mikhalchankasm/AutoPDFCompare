"""Core comparison engine for PDFCompare Local.

This package hosts the pure-Python building blocks (constants, dataclasses,
alignment, diff, classification, reporting). The top-level `compare_pdfs`
module re-exports the public symbols so existing imports keep working.
"""
